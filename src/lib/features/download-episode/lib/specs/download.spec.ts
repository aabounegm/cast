import { startEpisodeDownload } from '../download';

it.skip('starts downloading episodes', () => {
  startEpisodeDownload;
  // I honestly don't know how to test this function
  //  do we even need to test it?
});

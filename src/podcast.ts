export interface PodcastSeries {
  coverUrl: string;
  title: string;
  authors: string;
  website: string;
  description: string;
  episodesNumber: number;
}
export interface PodcastEpisode {
  title: string;
  decscription: string;
  duration: string;
}
